import { getHeartRateData, getSleepData } from '../../../src/utils/HealthConnectUtils';

describe('HealthConnectUtils', () => {
  it('fetches heart rate data correctly', () => {});

  it('fetches sleep data correctly', () => {});

  it('handles errors during heart rate data fetching', () => {});

  it('handles errors during sleep data fetching', () => {});

  it('parses heart rate data correctly', () => {});

  it('parses sleep data correctly', () => {});

  it('formats heart rate data for further processing', () => {});

  it('formats sleep data for further processing', () => {});
});
