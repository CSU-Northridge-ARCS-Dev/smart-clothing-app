import { getHeartRateData, getSleepData } from '../../src/utils/HealthConnectUtils';

describe('HealthConnectUtils', () => {
  it('should fetch heart rate data correctly between two dates', async () => { });

  it('should return an array of heart rate samples', async () => { });

  it('should handle errors when fetching heart rate data fails', async () => { });

  it('should fetch sleep data correctly between two dates', async () => { });

  it('should return an array of sleep stages', async () => { });

  it('should handle errors when fetching sleep data fails', async () => { });
});
